folder `testcases'
  here is only for development purposes.
  on deployment this folder has to be at the same level in the
  file structure as the `guitest.jar' file.

folder `presets'
  same as above.